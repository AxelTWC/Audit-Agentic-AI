# Audit Agent

An automated audit agent for streamlined auditing tasks.

## Structure

- `agent/` - Core agent implementation
  - `tools/` - Tool modules (search, file operations)
  - `agent.py` - Main agent class
  - `planner.py` - Planning module
- `data/` - Data storage directory
